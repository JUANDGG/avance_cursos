spring:
  application:
    name: seguridadConJwt
  datasource:
    url: jdbc:mysql://localhost:3306/users_auth
    username: root
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: none

server:
  port: 8086
